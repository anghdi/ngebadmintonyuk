---
paths:
  - 'resources/{views,js,css}/**'
---

# Viewsjscss

## Show Feed Studio connection history
Feed Studio admin UI must expose each batch's parent row, incoming/outgoing handoff coordinates and draft/exported/published status. Only unpublished batches show delete controls; successful export reveals publish and create-next-row actions.
