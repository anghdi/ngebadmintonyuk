---
paths:
  - 'app/{Actions,Services,Http}/**, resources/{js,views,css}/**'
---

# Jsviewscss

## Feed Studio rows preserve visual continuity
Feed Studio Seed Row is one connected 3×1 master canvas using the fixed three-part copy. Each row stores a top-edge court-line exit; the following row maps it to a matching bottom-edge incoming connection, and layout regeneration must preserve both endpoints. Keep semantic content and portrait photos inside one panel's 96px gutter safe zone; only background geometry may cross panel boundaries. Preserve Grid Preview and right-to-left Instagram upload order.
