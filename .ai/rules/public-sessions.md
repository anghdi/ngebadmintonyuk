---
paths:
  - 'resources/views/public-sessions/**'
---

# Public Sessions

## Signed-in schedules use the application layout
Guests view public play-session pages with layouts.public. Authenticated users view the same catalogue and detail pages with layouts.app so the sidebar and authenticated controls remain available.
