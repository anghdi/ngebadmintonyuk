@extends('layouts.app') @php($label=$type==='income'?'Pemasukan':'Pengeluaran') @section('title',$label) @section('content')
<div class="page-head"><div><h1>{{ $label }}</h1><p>Catat dan pantau seluruh {{ strtolower($label) }} komunitas.</p></div><a class="btn primary" href="{{ route($type.'s.create') }}">+ Tambah {{ $label }}</a></div><div class="card filters"><form><label>Dari<input type="date" name="start_date" value="{{ request('start_date') }}"></label><label>Sampai<input type="date" name="end_date" value="{{ request('end_date') }}"></label><label>Kategori<select name="category_id"><option value="">Semua</option>@foreach($categories as $c)<option value="{{ $c->id }}" @selected(request('category_id')==$c->id)>{{ $c->name }}</option>@endforeach</select></label><button class="btn dark">Terapkan</button></form></div><div class="card table-card"><table><thead><tr><th>Tanggal</th><th>Kategori</th><th>Keterangan</th><th>Total</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->date->translatedFormat('d M Y') }}</td><td><span class="badge {{ $type }}">{{ $item->category->name }}</span></td><td>{{ $item->description ?: '—' }}</td><td class="money">{{ rupiah($item->details_sum_amount) }}</td><td><a class="link" href="{{ route($type.'s.show',$item) }}">Lihat</a></td></tr>@empty<tr><td colspan="5" class="empty">Belum ada {{ strtolower($label) }}.</td></tr>@endforelse</tbody></table>{{ $items->links() }}</div>
@endsection
